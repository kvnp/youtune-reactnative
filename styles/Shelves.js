import { StyleSheet } from 'react-native';

export const shelvesStyle = StyleSheet.create({
    scrollView: {
        marginTop: 20
    },

    scrollContainer: {
        flexGrow: 1,
        justifyContent: "center"
    },
});