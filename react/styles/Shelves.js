import { StyleSheet } from 'react-native';

export const shelvesStyle = StyleSheet.create({
    scrollView: {
        display: "flex"
    },

    scrollContainer: {
        flexGrow: 1,
        justifyContent: "center",
        paddingBottom: 20,
    },
});