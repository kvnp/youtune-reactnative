import React from "react";
import { View } from "react-native";

export default (
    style,
    isPlaying,
    title,
    subtitle,

    onPrevious,
    onNext,
    onPress
) => {
    return (
        <View style={style}>

        </View>
    );
}